# タスク詳細ドロワー 技術仕様

最終更新: 2026-07-24
参照元: `docs/01要件定義・設計/要件定義書.md` / `docs/01要件定義・設計/基本設計書.md` / `DESIGN.md` / `AGENTS.md`

## 関連ファイルと関数

- `src/ClientRenderPanels.html`: `renderDrawer()`, `renderDependencyPanel()`, `renderDatePicker()`, `renderCalendarPanel()`。
- `src/ClientBindings.html`: `selectCalendarDay()`, `setDraftDate()`, `clearCalendarSelection()`。
- `src/ClientActions.html`: `openNode()`, `closeDrawer()`, `initializeDetailDraft()`, `saveDetail()`, `fitChildren()`。
- `src/01_NodeApi.js`: `saveNode()`, `getNodeActualDates()`, `fitNodeToChildren()`, `deleteNode()`, `restoreNode()`。

## draft管理

- 開いているノードIDは `state.drawerNodeId` です。
- `renderDrawer()` は `ancestorChain(node.id)` を逆順にし、案件ルートを除いた祖先と現在ノードを ` > ` で連結して `.detail-breadcrumb` に表示します。ルート以外の祖先がない場合はパンくずを描画しません。
- 編集中データは `state.detailDraft` に保持します。
- mutation反映後は `syncDetailDraftAfterMutation()` で開いているノードとの整合を確認します。
- 入力は `data-draft-scope="detail"` により `handleDraftInput()` へ集約されます。
- 日付はネイティブ `input[type=date]` ではなく `renderDatePicker()` のカスタムカレンダーで更新します。
- カレンダーは右端基準で開き、ドロワー右側からはみ出さないようにします。
- `setDraftDate()` は開始日選択時に終了日を同日へ補正し、終了日選択時は開始日より前の値を拒否します。
- `deliverable`, `note`, `includeInWbs`, `progress`, `actualStartDate`, `actualEndDate` も `detailDraft` に保持し、`saveDetail()` から `saveNodePatch()` へ渡します。
- 手動実績期間は開始・終了の両方または両方空欄だけを許可します。実績用カレンダーの片側選択はもう片側を補い、クリアは両方を空にします。
- `getNodeActualDates()` は詳細を開いた時だけActivityLogを読み、対象ノードの手動上書き前の自動算出値だけを返します。通常の全体同期レスポンスへActivityLog全件を含めません。
- デスクトップの `.detail-drawer-body` は主領域と約336pxの属性領域のCSS Gridです。960px以下では1列へ戻し、ネストしたスクロールを解除します。
- 進捗はカスタムドロップダウンで0/15/30/45/60/75/90/100を扱います。100を選んだ場合はクライアント側でも完了列へ寄せ、完了列から戻す場合は90へ戻します。
- 親ノードの進捗は読み取り表示です。親へのprogressパッチはサーバーで拒否します。
- 子が全削除されて親が末端に戻った場合は、過去に末端として保存された `Progress` が再び有効になります。親状態でのステータス変更は `Progress` を書き換えません。
- WBS反映チェックは、祖先に `includeInWbs === false` がある場合は無効化します。
- 通常保存は `saveDetail({ closeAfter: true })` で呼び、patch作成直後にドロワーを閉じてから `saveNodePatch()` を投げます。

## shellクローズ処理

- `prepareShellCloseAction()` がshellの状態を判定します。
- baselineと現在draftが同一で名前も空なら `deleteShellNode()` を呼びます。
- 名前が空で他フィールドが変更済みならドロワーを閉じず、名称欄へフォーカスします。
- 名前ありなら `saveNodePatch()` で通常保存します。

## 競合検知

- `saveDetail()` は `baseUpdatedAt: currentUpdatedAt(node.id)` を含めます。
- サーバーの `saveNode()` が現在のUpdatedAtと比較します。
- 不一致時は `makeConflictPayload_()` を返し、クライアントは最新ノードをマージして警告します。
