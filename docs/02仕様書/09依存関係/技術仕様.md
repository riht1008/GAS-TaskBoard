# 依存関係 技術仕様

最終更新: 2026-07-02
参照元: `docs/01要件定義・設計/要件定義書.md` / `docs/01要件定義・設計/基本設計書.md` / `DESIGN.md` / `AGENTS.md`

## 関連ファイルと関数

- `src/04_DependencyApi.js`: `addDependency()`, `deleteDependency()`, `validateDependency_()`, `rescheduleFromSeeds_()`, `topoSortSubset_()`。
- `src/ClientRenderPanels.html`: `renderDependencyPanel()`, `renderDependencyRow()`。
- `src/ClientActions.html`: `addPredecessor()`, `addSuccessor()`, `deleteDependencyConfirmed()`。
- `tests/dependency_api.test.mjs`: 合流、菱形、バッファ保持、循環検出の単体テスト。

## 検証

- active Nodesだけを対象にします。
- predecessor/successorは異なるNodeIdである必要があります。
- 両方がleafである必要があります。
- 両方にStartDate/EndDateが必要です。
- 既存Dependenciesとの重複を禁止します。
- 追加候補を含めたDAGに対してトポロジカルソートを行い、循環を検知します。

## 連鎖リスケジュール

- 保存系処理ではロック取得後に最新シート値を読み直します。
- seedから後続方向に辿った部分グラフを対象にします。
- トポロジカル順に処理します。
- 後続タスクのStartDateが全先行タスクの最大EndDateより前の場合だけ移動します。
- 移動量は崩れた分だけです。意図的な余白は詰めません。
- カスケードで変更したノードはUpdatedAt/UpdatedByも更新します。

## レスポンス

- 依存追加/削除後は、依存関係全体と影響ノードを返します。
- リスケジュール件数は `rescheduledCount` として返します。
